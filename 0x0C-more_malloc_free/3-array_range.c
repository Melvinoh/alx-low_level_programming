#include <stdlib.h>
#include "main.h"

/**
 * array_range - createsan aarray of integers ordered from 
 * @min: the first value of the array.
 * @max:the last value of the array.
 */
int *array_range(int min int max )
{
	if (min > max)
		return (NULL);
	size = max - min + 1;


