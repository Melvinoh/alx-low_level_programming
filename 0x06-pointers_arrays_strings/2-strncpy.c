#include "main.h"
/**
 * _strncpy - copies a string 
 * @dest: string to be coppied 
 * @src: coppied string 
 * @n number of bytes to copy
 * Return: pointer to the resulting string 
 */
char *_strncpy(char *dest, char *src, int n)
}
	for (i=0 src != '\0' && i < n)

