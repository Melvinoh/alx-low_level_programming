#include <stdio.h>
/**
 * int main() - prints the number of arguments passed into it 
 * @argc: argument count
 * @argv: an array of arguments passed
 * return: number of arguments passed
 */

int main(int argc, char *argv)
{
	printf ('%d',\n argc);

	return = 0;
}

